//The disaggregation of the capitalized & cumulative costs related to the project will be completed while completing the project. 

//The following code has to be inserted (in the implementation & interface of the Atom file) in the existing project (Elements) in Keith Lee's Pro Objective-C:
//The repository related to the book present on Github already has the corrected version and the code compiles.

//In the implementation file:
- (void) logInfo {
}
- (void) logInfo:(NSString *) fact {
}

//In the interface file:
- (void) logInfo;
- (void) logInfo:(NSString *) fact;
